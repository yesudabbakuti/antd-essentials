const Content = () => {
  return (
    <>
      <h1>Breadcrumb</h1>
      <p>
        {" "}
        A breadcrumb displays the current location within a hierarchy. It allows
        going back to states higher up in the hierarchy. When To Use# When the
        system has more than two layers in a hierarchy. When you need to inform
        the user of where they are. When the user may need to navigate back to a
        higher level.
      </p>
    </>
  );
};
export default Content;
